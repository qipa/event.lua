

agent_user = {
	save_version = {{unique = true,index = {uid = 1}}}
}

scene_user = {
	save_version = {{unique = true,index = {uid = 1}}},
	scene_info = {{unique = true,index = {uid = 1}}}
}

world_user = {
	save_version = {{unique = true,index = {uid = 1}}}
}

login_user = {
	save_version = {{unique = true,index = {uid = 1}}}
}

common = {
	
}